{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "chrome",
            "request": "attach",
            "port":9229,
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}